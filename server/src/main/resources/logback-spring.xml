<?xml version="1.0" encoding="UTF-8"?>
<!--
Providing this file makes Spring Boot skip its own default logback setup, so the
shared pieces are pulled back in by the includes below. The main file appender
(FILE -> logs/valomegle.log) and log levels stay driven by application.properties;
this file only adds the errors-only, date-labelled appender.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/file-appender.xml"/>

    <property name="LOG_DIR" value="logs"/>

    <!-- No <file> element: the dated pattern is itself the active file, so today's
         errors are written straight to logs/error-YYYY-MM-DD.log and appended to
         for the rest of the day; a new file is started at each midnight rollover. -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>${FILE_LOG_CHARSET}</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/error-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <root level="INFO">
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>
