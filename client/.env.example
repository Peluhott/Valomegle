# Leave these blank for local dev — the Vite dev server proxies /api and /ws to the
# backend (see vite.config.ts), so the frontend talks to the same origin as the page.
# Only set these for a production build where the frontend is served from a different
# origin than the API.
VITE_API_BASE_URL=
VITE_WS_BASE_URL=
